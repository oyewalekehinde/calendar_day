name: calendar_day
description: "This is a Widget that is use to customize a day date, by having a start and end date"
version: 0.0.1
homepage: https://github.com/oyewalekehinde/calendar_day

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
